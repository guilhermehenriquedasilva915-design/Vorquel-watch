# Claude Watch Skill

Reserved for the Claude Desktop orchestration skill.

The active skill must expose only the frozen narrow MCP tool set. Do not add Bash, arbitrary filesystem reads, raw path arguments, arbitrary URL arguments, credentials, cookies or unrestricted output destinations.
